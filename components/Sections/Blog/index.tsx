import React from 'react'
import SectionWrapper from '@/components/Sections/SectionWrapper'

export default function BlogSection(): React.ReactElement {
    return <SectionWrapper id='blog' title='Bloglar' />
}
